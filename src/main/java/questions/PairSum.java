package questions;

public class PairSum {

    /**
     * Finds and prints all pairs of numbers in an array that sum up to a target
     * value.
     * <p>
     * Restriction: Do not use Collections (like HashMap or ArrayList) to solve the
     * problem.
     * The solution should only use arrays and sorting.
     *
     * @param nums   The array of integers.
     * @param target The target sum that the pairs should reach.
     * @return A string containing the found pairs, formatted as "(n1, n2)".
     *         Returns an empty string if no pair is found.
     */
    public String findPairs(int[] nums, int target) {
        // TODO: Implement your logic here.
        return "";
    }
}