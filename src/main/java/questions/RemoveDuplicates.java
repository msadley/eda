package questions;

public class RemoveDuplicates {

    /**
     * Removes duplicate elements from a sorted array and returns the new
     * size of the array.
     * <p>
     * Restriction: The modification must be done in-place, without
     * creating a new array.
     * The method must work with O(1) space complexity.
     *
     * @param nums A sorted array of integers.
     * @return The new size of the array without duplicates.
     */
    public int remove(int[] nums) {
        // TODO: Implement your logic here.
        return 0;
    }
}