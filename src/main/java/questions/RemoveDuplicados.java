package questions;

public class RemoveDuplicados {

    /**
     * Remove os elementos duplicados de um array ordenado e retorna o novo
     * tamanho do array.
     * <p>
     * Restrição: A modificação deve ser feita no próprio array (in-place), sem
     * criar um novo.
     * O método deve funcionar com complexidade de espaço O(1).
     *
     * @param nums Um array de inteiros ordenado.
     * @return O novo tamanho do array sem duplicados.
     */
    public int remover(int[] nums) {
        // TODO: Implemente sua lógica aqui.
        return 0;
    }
}
