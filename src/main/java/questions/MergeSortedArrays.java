package questions;

public class MergeSortedArrays {

    /**
     * Combines two sorted arrays into a single sorted array.
     * <p>
     * Restriction: The solution must be implemented without using the language's
     * sorting function (like Arrays.sort()) after the combination.
     * The method must have a linear time complexity O(n + m), where n and m are
     * the sizes of the arrays.
     *
     * @param nums1 The first sorted array.
     * @param nums2 The second sorted array.
     * @return A new array containing all the elements of nums1 and nums2 in
     *         ascending order.
     */
    public int[] merge(int[] nums1, int[] nums2) {
        // TODO: Implement your logic here.
        return new int[0];
    }
}