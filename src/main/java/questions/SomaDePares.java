package questions;

public class SomaDePares {

    /**
     * Encontra e imprime todos os pares de números em um array que somam a um
     * valor-alvo.
     * <p>
     * Restrição: Não use Collections (como HashMap ou ArrayList) para resolver o
     * problema.
     * A solução deve usar apenas arrays e ordenação.
     *
     * @param nums  O array de números inteiros.
     * @param alvo A soma alvo que os pares devem alcançar.
     * @return Uma string contendo os pares encontrados, formatada como "(n1, n2)".
     *         Retorna uma string vazia se nenhum par for encontrado.
     */
    public String encontrarPares(int[] nums, int alvo) {
        // TODO: Implemente sua lógica aqui.
        return "";
    }
}
